global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "${job_name}"
    static_configs:
      - targets: ["${service.service}:${service.port}"]
    scrape_interval: 60s
